TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
TELEGRAM_PERMISSION_TIMEOUT=300
TELEGRAM_FALLBACK_ON_ERROR=allow
TELEGRAM_HOOK_LOG=/tmp/telegram_claude_hook.log

# Smart filtering: all | smart (default) | critical
# - all: every permission goes to Telegram (v0.3 behavior)
# - smart: auto-approves safe commands (ls, cat, git status...), Telegram for dangerous ones
# - critical: only the most destructive operations (rm, sudo, git push...) go to Telegram
TELEGRAM_SENSITIVITY=smart

# Dangerous operations flow:
# LOCAL_DELAY=0  -> Telegram directly with buttons (no terminal prompt)
# LOCAL_DELAY=60 -> Terminal prompt first, Telegram after 60s if no response
#
# When LOCAL_DELAY > 0:
#   1. Claude Code shows normal terminal permission prompt
#   2. If you respond in terminal -> done (no Telegram)
#   3. If no response after LOCAL_DELAY seconds -> Telegram with buttons
#   4. If you tap Allow/Deny on Telegram -> sends keystroke to terminal
TELEGRAM_LOCAL_DELAY=60
